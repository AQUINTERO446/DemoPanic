<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"    
             x:Class="DemoPanic.Views.UbicationsPage"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Title="Ubicaciones">
    <ContentPage.Content>
        <StackLayout
            BindingContext="{Binding Ubications}">
            <maps:Map 
                x:Name="MyMap"
                IsShowingUser="true"
                MapType="Street">
            </maps:Map>
            <Label
                x:Name="UserLocationLabel"
                FontAttributes="Bold"
                FontSize="Large"
                TextColor="Black"/>
            <Button
                x:Name="RefreshButton"
                Text="Refrescar Ubicacion"
                Clicked="RefreshLocation_Clicked"/>
        </StackLayout>

    </ContentPage.Content>
</ContentPage>